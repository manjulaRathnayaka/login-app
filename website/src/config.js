
const config = {
  regionLoginAppUrl: 'http://localhost:3000', // URL to the region-login-app
  // When deploying to production, you would update this URL to the deployed region-login-app
  // Example: regionLoginAppUrl: 'https://region-login.example.com',

  // You can add other configuration options for the website app here
};

export default config;
